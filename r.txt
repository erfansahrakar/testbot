# کتابخانه‌های اصلی ربات تلگرام
python-telegram-bot==20.7

# کتابخانه‌های نمودار و گراف
matplotlib==3.8.2
numpy==1.26.3
python-dotenv==1.0.0
psutil==5.9.6

# دیتابیس (SQLite داخلی پایتون است)
# اما در صورت نیاز به PostgreSQL یا MySQL:
# psycopg2-binary==2.9.9
# PyMySQL==1.1.0

# اختیاری: برای بهبود عملکرد
# pillow==10.1.0  # اگر می‌خواهید تصاویر را پردازش کنید
